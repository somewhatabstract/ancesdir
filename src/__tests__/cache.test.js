// @flow
import cache from "../cache.js";

describe("cache", () => {
    it("should return a map", () => {
        // Arrange
        // Act
        // Assert
        expect(cache).toBeInstanceOf(Map);
    });
});
